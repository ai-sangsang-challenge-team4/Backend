# Windows Korean locale + JDK 17: Gradle worker argument files must use
# the native encoding to preserve the Korean characters in the project path.
org.gradle.jvmargs=-Xmx512m -XX:MaxMetaspaceSize=384m -Dfile.encoding=x-windows-949
